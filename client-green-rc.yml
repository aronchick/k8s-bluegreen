apiVersion: v1
kind: ReplicationController
metadata:
  name: client-green
spec:
  replicas: 2
  selector:
    name: client
    version: v3.0.0
  template:
    metadata:
      labels:
        name: client
        version: v3.0.0
    spec:
      containers:
      - image: gcr.io/linear-pointer-95422/client_image:v3.0.0
        name: client
        livenessProbe:
        # an http probe
          httpGet:
            path: /_status/healthz
            port: 3000
        ports:
        - containerPort: 3000
          protocol: TCP
