apiVersion: v1beta3
kind: ReplicationController
metadata:
  name: client
spec:
  replicas: 2
  selector:
    name: client
    version: v1.0.2
  template:
    metadata:
      labels:
        name: client
        version: v1.0.2
    spec:
      containers:
      - image: gcr.io/mythical-willow-91020/client_image:v1.0.2
        name: client
        ports:
        - containerPort: 80
          protocol: TCP
