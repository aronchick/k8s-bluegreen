apiVersion: v1beta3
kind: Service
metadata:
  labels:
    name: dashboard
  name: dashboard
spec:
  ports:
    - port: 80
      targetPort: 3000
  selector:
    name: dashboard
  createExternalLoadBalancer: true
